{
  "name": "iot-dashboard-platform",
  "version": "1.0.0",
  "description": "IoT Dashboard Platform - Local Development Helper Scripts",
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "setup:backend": "cd backend && npm install && node src/scripts/initDatabase.js && npm run seed",
    "setup:frontend": "cd frontend && npm install",
    "dev:backend": "cd backend && npm run dev",
    "dev:frontend": "cd frontend && npm start",
    "init:db": "cd backend && node src/scripts/initDatabase.js",
    "seed:db": "cd backend && npm run seed",
    "test:login": "node test-login.js"
  },
  "keywords": ["iot", "dashboard"],
  "author": "",
  "license": "ISC"
}

